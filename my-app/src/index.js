import ReactDOM from "react-dom";
import App from "./App.jsx";

//Esta es la forma de renderizar elementos en el document desde la librería ReactDOM
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
